package org.galegofer.phone.test.controller;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class WeatherControllerTest {
    /**
     * A couple of tests using WebTestClient against this endpoint,
     * trying to check for invalid parameters, service getting called as expected, exceptions getting handled properly.
     */
}