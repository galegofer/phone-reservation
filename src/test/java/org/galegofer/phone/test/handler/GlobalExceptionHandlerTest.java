package org.galegofer.phone.test.handler;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class GlobalExceptionHandlerTest {
    /**
     * Will test all the different exception handlers to see how they
     * are converting the runtime exceptions into errors to be displayed to the user,
     * without showing implementations or errors details of the exception itself.
     */
}